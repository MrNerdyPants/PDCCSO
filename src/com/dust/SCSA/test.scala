package com.dust.SCSA
 import Goto._
 
 
 
object test {
/*
  var sum = 0
  var i = 0
  val beforeLoop = label // Create label for goto
  if (i < 10000) {
    println(i)
    sum += i
    i += 1
    goto(beforeLoop) // Jump to label
  }
  println(sum)
*/
}